sources:
  - name: raw
    schema: "{{ 'main' if target.name in ['dev', 'prod'] else 'public' }}"
    tables:
      - name: raw_laus_series
      - name: raw_laus_data
      - name: raw_oews_series
      - name: raw_oews_data
      - name: raw_acs_data